#include <stdio.h>
#include <math.h>

int main()
{
	long long int num,l,k=1,r=pow(10,9)+7;
	scanf("%lld", &num);    
	int a[num];
	for(int i=0;i<num;i++)
	{
		scanf("%d",&a[i]);
		k=(k*a[i])%r;
	
	}
	printf("%lld",k);
}
