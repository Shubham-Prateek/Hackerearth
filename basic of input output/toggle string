#include <stdio.h>

int main()
{
	char s[100];
	scanf("%s", s);
	for(int i=0;i<strlen(s);i++)
	{
		if(s[i]>64&&s[i]<91)
			s[i]=s[i]+32;
		else if(s[i]>96&&s[i]<123)
			s[i]=s[i]-32;
	}
	printf("%s",s);
	return 0;
}
