#include <stdio.h>

int main()
{
	int l,n;
	scanf("%d %d", &l,&n);
	for(int i=0;i<n;i++)
	{
		int w,h;
		scanf("%d %d",&w,&h);
		if(w!=h)
		{
			if(w<l||h<l)
			printf("UPLOAD ANOTHER\n");
			else
			printf("CROP IT\n");
		}
		else if(w==h)
			{
				if(w<l)
				printf("UPLOAD ANOTHER\n");
				else
				printf("ACCEPTED\n");

			}
	}
	return 0;
}
