#include <stdio.h>
#include<string.h>

int main()
{
	char s[1000];
	int i,j,l=0;
	scanf("%s", s);
	int k=strlen(s)-1,p=0; 
	//printf("%s",s);             			
	while(s[k]==s[p]&&k>p)
	{
		k--;
		p++;
	}
	
	if(k+1==p||k==p)
	printf("YES");
	else
	printf("NO");
	return 0;
}
