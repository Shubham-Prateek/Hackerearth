#include <stdio.h>

int main(){
	int num;
	scanf("%d", &num);  
	for(int i=0;i<num;i++)
	{
		int n,x,s=0,l=0,p=0;
		scanf("%d %d",&n,&x);
		//int p=x;
		while(s<x)
		{
			l++;
			p++;
			if(p>n)
				p=1;
			s=s+l;
		}
		printf("%d\n",p);
	}
}
